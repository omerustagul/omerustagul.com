Marketplace digital-product card: cover + "Dijital Ürün" tag + title + seller + format + price.

```jsx
<ProductCard title="Grid UI Kit" seller="Marka Studio" format="Figma" price="59 USD" />
```

`priceNote` (default "'den") suffixes the price → "59 USD'den".