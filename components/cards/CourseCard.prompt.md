Academy course card: cover + level chip + title + instructor + rating + price.

```jsx
<CourseCard title="Tasarım Sistemi" instructor="Deniz Arı" rating={4.9} reviews={214} price="₺1.299" level="Orta" />
```