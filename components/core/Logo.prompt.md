The Marka wordmark; use in headers/footers. Defaults its text to BRAND_NAME (single source of truth).

```jsx
<Logo href="/" size="md" />
<Logo size="xl" showDot={false} />
```

Sizes sm/md/lg/xl. `showDot` toggles the accent-green trailing dot. Pass `name` only to override the brand name locally.