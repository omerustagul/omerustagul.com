Mono-cased eyebrow/label; use for tags like "PROJE", "DİJİTAL ÜRÜN", or status.

```jsx
<Badge variant="solid">PROJE</Badge>
<Badge variant="muted" dot>Yayında</Badge>
```

Variants: solid (green), outline, muted, invert. `dot` adds a leading status dot.