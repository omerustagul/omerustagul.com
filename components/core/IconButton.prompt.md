Icon-only round button; use for nav arrows, social, theme toggle, close.

```jsx
<IconButton variant="solid" label="next">→</IconButton>
<IconButton variant="ghost" label="more">＋</IconButton>
```

Variants outline/solid/ghost; sizes sm/md/lg. Always pass `label` (aria-label).